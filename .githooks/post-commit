#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"

echo "Commit completed. Creating a debug update APK..."

if ! "$repo_root/scripts/build-debug-update.sh"; then
    echo "The APK build or phone transfer failed. The Git commit was already created." >&2
    exit 1
fi
